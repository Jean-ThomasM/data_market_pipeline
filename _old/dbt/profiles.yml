data_market_pipeline_old:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "_old/data/geo.sqlite"
      schema: main
      schemas_and_paths:
        main: "_old/data/geo.sqlite"
      schema_directory: "_old/data"
